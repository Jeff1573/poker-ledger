# 微信小程序配置(必填)
WECHAT_APPID=
WECHAT_APPSECRET=

# JWT 密钥(生产环境必须修改为强随机值)
JWT_SECRET=请修改为强随机密钥-使用openssl-rand-base64-32生成

# 服务端口(内部使用,无需修改)
PORT=3000

# 数据库路径(使用默认值即可)
SQLITE_FILE=/app/data/db.sqlite

# 后台管理(首次启动会自动生成随机密码；需要重置时启动一次设置为 1)
ADMIN_RESET_PASSWORD=0
ADMIN_COOKIE_SECURE=0
